# Biodiversity Change Detection

Frontend-only React prototype for ESRS E4 biodiversity checking workflows.


## Run locally
```bash
npm install
npm run dev
```

> Note: This prototype uses mocked data only (no backend/API).
